1. Посмотрел с помощью команды cd

2. С помощью команды cat можно выводить содержимое файла как в консоль, так и передавать в другие файлы.
С помощью команды mv можно как перемещать файлы и папки по директориям, так и переименовывать.

3. Команда touch создает файл. Команда mkdir создает директорию.
Команды rm и unlink удаляют файлы. Если к команде rm добавить ключ -r, то можно будет так же удалить и директорию. С ключом -f удаляет так же
вложенные файлы и папки.

4. В домашней директории 12 скрытых файлов. 
	$find ~/ -maxdepth 1 -type f -name '.*' | wc -l
   и 9 скрытых директории
	$find ~/ -maxdepth 1 -type d -name '.*' | wc -l

5. Вывод ошибок в отдельный файл. 130 ошибок. 
$cat /etc/* 2> err.txt

   Если нужно вывести и ошибки и выполненный вывод в один файл.
$cat /etc/* &> err.txt

   Если нужно вывести ошибки и выполненный вывод в разные файлы.
$cat /etc/* 1> sucssess.txt 2> err.txt

6. Запустил игру Солитер - процесс sol.
Открыл ctrl + alt + f2 другой терминал и через $ps aux | grep sol нашел нужный процесс.
$kill -9 4898 - закрыл через PID.

7. Команда для просмотра используемых девайсов
$lsof /dev| less

8. Директории для фотографий
$mkdir -p {2015..2020}/{1..12}

9. Заполняем директории файлами вида yyyymmdd.txt
$for y in {2000..2020}
> do
> for m in {01..12}
> do
> for d in {01..30}
> do
> mkdir -p $y/$m/$d
> echo $y$m$d.txt > $y/$m/$d/$y$m$d.txt
> done
> done
> done

10.  Использовать команду cut на вывод длинного списка каталога, чтобы отобразить только права доступа к файлам. 
Затем отправить в конвейере этот вывод на sort и uniq, чтобы отфильтровать все повторяющиеся строки. 
Потом с помощью wc посчитать различные типы разрешений в этом каталоге. 
Самостоятельно решить задачу, как сделать так, чтобы в подсчет не добавлялись строка Итого и файлы . и .. 
(ссылки на текущую и родительскую директории)

Текущая директория:
$ls -lA | cut -d ' ' -f 1 | sort | uniq | head -n -1

или

$ls -lA | cut -b 1-10 | sort | uniq | head -n -1

Любая другая, после ключей указываем путь: 
$ls -lA / | cut -d ' ' -f 1 | sort | uniq | head -n -1
